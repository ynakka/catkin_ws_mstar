<launch>
    <!-- Spacecraft Name -->
    <arg name="spacecraft_name"/> 
    <!-- Navigation Node -->
    <node name="navigation_node_$(arg spacecraft_name)" pkg="mstar_navigation" type="navigation_main.py" args="$(arg spacecraft_name)"></node>
    <!-- Bag Data -->
</launch>